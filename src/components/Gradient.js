import React from "react";
import "../assets/css/Gradient.css";

/**
 * The gradient background on the landing page.
 * All the values are set in Gradient.css
 */
export default function Gradient() {
    return (
        <div id="gradient"></div>
    );
}